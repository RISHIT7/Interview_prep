SELECT Column1, Column2
FROM Schema.TableName
WHERE {Filtering Condition}
GROUP BY [Column Name]
HAVING [Aggregate Function] {comparison operator} [filtering criteria]
ORDER BY {Column Name, Ordinal, Alias}
